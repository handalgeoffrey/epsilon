/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  basePath: '/epsilon',
  assetPrefix: '/epsilon',
};

export default nextConfig;
